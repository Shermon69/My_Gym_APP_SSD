=== V4 - Hardcoded JWT and Session Secrets - AFTER evidence ===
Captured: 2026-09-26T10:26:49+05:30
Baseline commit: 636e180400214c6e8882b46d868bac097b2e06f2

--- 1. Search tracked server files for the exposed JWT secret ---
Command: git grep -n -F 'the-a-team-is-perfect' -- server

Output:
No matches in tracked server files.

--- 2. Verify secrets are read from environment variables ---
Command: git grep -n -E 'JWT_SECRET|SESSION_SECRET' -- server

Output:
server/.env.example:4:JWT_SECRET=your-jwt-secret-here
server/.env.example:5:SESSION_SECRET=your-session-secret-here
server/index.js:9:if (!process.env.JWT_SECRET || !process.env.SESSION_SECRET) {
server/index.js:10:  console.error("FATAL: JWT_SECRET and SESSION_SECRET must be set in .env");
server/index.js:81:    secret: process.env.SESSION_SECRET,
server/src/config/auth.config.js:2:    secret: process.env.JWT_SECRET

The server loads dotenv before validating that both variables are set.
JWT signing and verification use the configured JWT_SECRET. Express sessions
use SESSION_SECRET.

--- 3. Verify local environment values are present ---
Check: server/.env contains non-empty JWT_SECRET and SESSION_SECRET entries.
Output:
JWT_SECRET nonempty=True
SESSION_SECRET nonempty=True

Secret values intentionally omitted from this evidence.

--- 4. Outcome and residual security action ---
The hardcoded secret has been removed from the current tracked server source.
Keep the real secret values in local server/.env and do not commit that file.
The server/.env.example currently contains placeholders only.

The exposed JWT secret remains in Git history. Ensure JWT_SECRET and
SESSION_SECRET are newly generated values (not the previously exposed values),
and rotate them because changing current source does not invalidate a secret
that was disclosed in repository history. Secret strength and successful live
server startup were not tested as part of this evidence capture.
